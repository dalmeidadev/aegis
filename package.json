{
  "name": "aegis",
  "version": "0.1.0",
  "private": true,
  "module": "src/index.ts",
  "type": "module",
  "main": "./dist/index.cjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build:js && bun build:types",
    "build:js": "bun build ./src/index.ts --outdir ./dist --target node --format esm,cjs",
    "build:types": "tsc --emitDeclarationOnly --outDir dist",
    "test": "vitest run",
    "test:watch": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "biome lint ./src",
    "format": "biome format --write ./src",
    "check": "biome check ./src",
    "check:apply": "biome check --apply ./src"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/axios": "^0.14.4",
    "@vitest/coverage-v8": "3.0.9",
    "bun-types": "latest",
    "vitest": "^3.0.9"
  },
  "peerDependencies": {
    "axios": "",
    "typescript": "^5"
  },
  "peerDependenciesMeta": {
    "axios": {
      "optional": true
    }
  }
}
